<<<<<<< HEAD
#include <stdio.h>
/*
 if문 기본 문법
     if(con) {
     } else if(con) {
     } else if(con) {
     } else {
     실행문;
     }

     - if(con)으로 시작
     - if와 else는 1번만 사용 가능, else if 반복 사용 가능
     - if와 else if의 조건이 참이면 해당 block문 실행
     - if(con)에서 시작해서 하행
         - con F일 시 : 다음 else if 실행 
         - con T일 시 :  block문 실행 후 break
     - else는 모든 con을 만족하지 못하는 경우 실행 

 if문 조합
     1. if()
     2. if()-else
     3. if()-else if()
     4. if()-else if()-else
*/
int main(){

=======
#include <stdio.h>
/*
 if문 기본 문법
     if(con) {
     } else if(con) {
     } else if(con) {
     } else {
     실행문;
     }

     - if(con)으로 시작
     - if와 else는 1번만 사용 가능, else if 반복 사용 가능
     - if와 else if의 조건이 참이면 해당 block문 실행
     - if(con)에서 시작해서 하행
         - con F일 시 : 다음 else if 실행 
         - con T일 시 :  block문 실행 후 break
     - else는 모든 con을 만족하지 못하는 경우 실행 

 if문 조합
     1. if()
     2. if()-else
     3. if()-else if()
     4. if()-else if()-else
*/
int main(){

>>>>>>> 5b92c22f7f5aeb5b62ebdb9a3235f5928f181879
}