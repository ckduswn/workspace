/*
Ctrl제어문
 1. 조건문
     - if-else-else
     - switch-case-break
 2. 반복문
     - for   : 반복횟수 아는 경우 
     - while : 반복횟수 모르는 경우 
 3. 분기문 
     - cf) break & continue : 반복문에서 사용 
     - break                : 반복문에서 빠져나가기 
     - continue             : 다음 반복으로 넘기기 
     - return               : 함수(함수 종료의 의미, return 값 : 호출한 곳으로 해당 값 전달)
*/